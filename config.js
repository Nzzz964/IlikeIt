module.exports = {
    /**
     * 自定义 Secret 验证 默认 true
     * Is the given secret legal
     * @param {string} secret
     * @returns {boolean}
     */
    valid(secret) {
        return true;
    }
}